# Architecture Decisions Log

All major decisions recorded with rationale and date.

| # | Decision | Rationale | Date |
|---|----------|-----------|------|
| 1 | SDK over headless CLI | Less overhead, better parallelism, native streaming, multi-model support | 2026-01-14 |
| 2 | New app, not modified Automaker | Reuse architectural patterns, skip complexity debt | 2026-01-14 |
| 3 | Multi-workflow architecture | Future-proof for auto-design, auto-market, etc. | 2026-01-14 |
| 4 | Standalone orchestrator, not plugin | Plugin coupling is fragile; autonomous loop needs full control | 2026-01-14 |
| 5 | Plan/Execute phase separation | Strict role boundaries proven by oh-my-opencode's Prometheus/Atlas | 2026-01-14 |
| 6 | Agent role-based tool restrictions | Read-only exploration agents prevent scope creep | 2026-01-14 |
| 7 | Provider fallback chains | 3-step resolution (user, chain, default) is proven and pragmatic | 2026-01-14 |
| 8 | Python for core engine | Agent ecosystem is Python-first; all candidate frameworks are Python-native | 2026-02-11 |
| 9 | TypeScript only for UI | Dashboard/web UI layer, separate concern from orchestration engine | 2026-02-11 |
| 10 | No custom provider abstraction | Both Pydantic AI and Google ADK handle multi-model natively; building our own is unnecessary | 2026-02-11 |
| 11 | Claude Agent SDK rejected | It is an agent harness (single Claude agent), not a workflow orchestrator; Claude-only, TS-only | 2026-02-11 |
| 12 | Google ADK selected as framework | Unified composition of LLM agents + deterministic tools; first-class workflow primitives | 2026-02-11 |
| 13 | Phased MVP delivery | Targeting all 15+ features simultaneously risks bloat; MVP focuses on 6 core capabilities | 2026-02-11 |
| 14 | Skills system as Phase 1 component | Agents without skills are generic; skills produce project-appropriate output from day one | 2026-02-11 |
| 15 | Workflow composition system as Phase 1 | Workflows must be pluggable from day one; hardcoding auto-code then bolting on others later would require ripping out assumptions | 2026-02-11 |
| 16 | MCP used sparingly | MCPs add significant context bloat; prefer lightweight FunctionTools; use agent-browser for browser automation | 2026-02-11 |
| 17 | LLM Router for dynamic model selection | Different tasks benefit from different models; route by capability/cost/speed, not hardcoded model strings | 2026-02-11 |
| 18 | ADK App class as application container | App provides lifecycle management, context compression, resumability, plugin registration -- use as the top-level container | 2026-02-11 |
| 19 | Multi-level memory as Phase 1 | Agents must accumulate learnings across deliverables and sessions; without memory, deliverable 47 cannot know what patterns deliverables 1-10 established | 2026-02-11 |
| 20 | API-first gateway architecture | FastAPI gateway owns the external contract; ADK behind anti-corruption layer; swappable without client changes | 2026-02-11 |
| 21 | REST + SSE transport | REST for commands/queries, SSE for real-time event streaming; no GraphQL or gRPC at gateway layer; WebRTC reserved for future voice | 2026-02-11 |
| 22 | Single database | All data lives behind the gateway; no separate dashboard database; SQLAlchemy 2.0 async + Alembic migrations | 2026-02-11 |
| 23 | Redis from day one | Task queue (ARQ), event bus (Redis Streams), cron store, cache -- fundamental infrastructure, not a Phase 2 add-on | 2026-02-11 |
| 24 | ARQ for async workers | Native asyncio worker (not Celery); out-of-process workflow execution; gateway enqueues, workers execute | 2026-02-11 |
| 25 | Dashboard as pure SPA | React 19 + Vite static build; consumes gateway API via REST + SSE; no backend, no database of its own | 2026-02-11 |
| 26 | hey-api codegen for type safety | Pydantic models -> FastAPI -> OpenAPI spec -> hey-api -> typed TS client + TanStack Query hooks; build-time type safety without TS ORM | 2026-02-11 |
| 27 | Redis Streams for event bus | Persistent, replayable, consumer groups; SSE reconnection via Last-Event-ID + stream replay; webhook dispatch via consumer | 2026-02-11 |
| 28 | ARQ cron for scheduled jobs | Built-in to ARQ; no separate scheduler service needed | 2026-02-11 |
| 29 | Director (LlmAgent, opus) as root_agent | Cross-project governance requires LLM reasoning; ADK App.root_agent is the natural home. (Originally "permanent"; superseded by #40: stateless config, recreated per invocation.) | 2026-02-14 |
| 30 | PMs (LlmAgent, sonnet) for per-project management | Autonomous project supervision requires reasoning, not just programmatic orchestration | 2026-02-14 |
| 31 | Recursive autonomy at every tier | Each tier handles its problems; escalation is the exception | 2026-02-14 |
| 32 | Director has full project observability | Can intervene when patterns go wrong; not blind delegation | 2026-02-14 |
| 33 | Hard limits cascade CEO → Director → PM | Resource governance follows the hierarchy | 2026-02-14 |
| 34 | 6-level memory applied per tier scope | Original memory architecture maps naturally to hierarchical supervision | 2026-02-14 |
| 35 | All hierarchy is MVP scope | Not deferred; Director + PMs built in Phase 5, not Phase 8a+ | 2026-02-14 |
| 36 | Tool/Agent terminology aligned with ADK taxonomy | ADK separates tools (passive, LLM-discretionary) from agents (active, pipeline-structured); our docs conflated them | 2026-02-16 |
| 37 | Skills adopts Agent Skills open standard file format | Interoperability with emerging standard; deterministic runtime stays custom | 2026-02-16 |
| 38 | PM IS the outer loop -- no separate orchestrator agent | Single-use abstraction; PM needs inter-batch reasoning; mechanical parts become tools | 2026-02-16 |
| 39 | Batch oversight via PM tools + deterministic safety mechanisms | PM manages strategy; `after_agent_callback` for `verify_batch_completion`; `checkpoint_project` = `after_agent_callback` on DeliverablePipeline (persists state via `CallbackContext`); `run_regression_tests` = `RegressionTestAgent` (CustomAgent) in pipeline after each batch (reads PM regression policy from session state) | 2026-02-16 |
| 40 | Director stateless -- config object recreated per invocation | All state in DB via DatabaseSessionService; formation artifacts in `user:` scope (three structured keys: identity, CEO profile, operating contract — see #68). Director acts as COO + personal assistant (Phase 5). | 2026-02-16 |
| 41 | Director→PM delegation via `sub_agents` + `transfer_to_agent` | Director's LLM reasons about when/which PM to activate; AgentTool rejected (forces synchronous execution); declarative tree rejected (removes Director reasoning) (Phase 5) | 2026-02-16 |
| 42 | Multi-session architecture for Director | Chat sessions for CEO interaction, work sessions per project for background autonomous oversight; same agent definition, different session IDs; multiple chats per project, "Main" is permanent project; cross-session bridge via `app:`/`user:` state + MemoryService + Redis Streams; no new infrastructure needed (Phase 5) | 2026-02-16 |
| 43 | Unified CEO queue | Single DB-backed queue for notifications, approvals, escalations, and tasks; types + priority + structured metadata per item; SSE real-time push; not injected into chats, click-through opens project chat (Phase 5) | 2026-02-16 |
| 44 | PM lifecycle same as Director -- stateless config object | Session continuity in DB; consistent lifecycle across all agent tiers (Phase 5) | 2026-02-16 |
| 45 | No new ADK scope for project config | Project config as DB entity (project table with CRUD/permissions/versioning); existing 4 ADK scopes sufficient: `app:` = global, `user:` = CEO prefs + Director formation artifacts, session = per-session, `temp:` = scratch (Phase 5) | 2026-02-16 |
| 46 | ADK-native tool authorization via GlobalToolset | **Revised** from directory-scoped tiers to ADK-native `GlobalToolset(BaseToolset)`. Tools as Python functions in `app/tools/` organized by function type. `get_tools(readonly_context)` for per-role vending. Cascading permission config: CEO→Director→PM→Worker. Director can author new tools, CEO approval required by default. Original design (tools/director/, tools/pm/, tools/worker/, tools/shared/) reinvented what ADK provides natively. (Phase 4-5) | 2026-02-16 |
| 47 | Deterministic safety ops | Checkpoint always-automatic (after every deliverable); regression testing policy-enforced (PM defines strategy at project start, system enforces); PM-discretionary: `select_ready_batch()`, status queries, escalation (Phase 5) | 2026-02-16 |
| 48 | Skills standard mapping via `metadata.*` extension fields | AutoBuilder extension fields under agentskills.io spec's official `metadata.*` extension point: `metadata.triggers`, `metadata.tags`, `metadata.applies_to`, `metadata.priority`; third-party skills without triggers work via `description` keyword fallback (Phase 5-6) | 2026-02-16 |
| 49 | Gateway implements two distinct interaction models | Work-queue (`/workflows`, `/specs`) for fire-and-forget async execution; Conversational (`/chat`) for session-oriented Director interface with SSE streaming. These are architecturally distinct — conflating them misrepresents the Director interaction model. `Chat` + `ChatMessage` DB models required. (Phase 5 pre-work) | 2026-02-27 |
| 50 | InstructionAssembler — fragment-based prompt composition | 6 typed fragments (safety, identity, governance, project, task, skill) via InstructionAssembler; auditability via source tracking; enables Director governance without prompt rewriting. Research: OpenAI instructions-as-function, modular prompt engineering (13x iteration speedup), Claude Code micro-fragment architecture. See #55 for SAFETY fragment details. | 2026-03-07 |
| 51 | Declarative agent definition files — AgentRegistry | Agent definitions as markdown files with YAML frontmatter + AgentRegistry for dynamic instantiation; shared infra for workflow composability (Decision #15); CustomAgents skip instruction/model assembly. Research: LangGraph Send API, CrewAI agent-as-config, OpenAI Agents SDK lightweight objects. See #54 for file format and cascade details. | 2026-03-07 |
| 52 | Context Recreation over Compaction | When context budget threshold hit: persist to memory → fresh session → reassemble via InstructionAssembler + skills + memory tools; lossless alternative to lossy compaction summarization; InstructionAssembler serves dual purpose (initial construction + recreation). | 2026-03-07 |
| 53 | System Reminders — ephemeral conversation nudges | Soft nudges (token budget, state changes) injected as tagged messages via before_model_callback; NOT for hard governance (governance in assembled system prompt); ephemeral by design — lost during recreation is acceptable. Inspired by Claude Code's <system-reminder> pattern. | 2026-03-07 |
| 54 | Declarative agent definition files | Agent definitions are markdown files with YAML frontmatter (not Python dataclasses). Body = instruction content, frontmatter = metadata (tool_role, model_role, type, etc.). **3-scope file cascade**: global → workflow → project (runtime state is a separate mechanism, not a file scope). Full replacement by name. Evolves Decision #51 — AgentRegistry scans files instead of receiving Python registrations. Research: Claude Code markdown agents, CrewAI YAML (identity vs code split), ADL (definition ≠ execution), Kustomize/CSS cascade models. | 2026-03-09 |
| 55 | SAFETY fragment formalized (extends #50) | Formalizes the non-overridable safety floor in InstructionAssembler: no data exfiltration, tool boundary enforcement, escalation protocol. Cannot be overridden by any scope. Distinct from GOVERNANCE (agent-specific, overridable via cascade). SAFETY is the floor; GOVERNANCE is the ceiling. | 2026-03-10 |
| 56 | Hybrid CustomAgent model formalized (extends #12) | Formalizes the always-intended spectrum: purely deterministic (SkillLoaderAgent, LinterAgent) or hybrid (DependencyResolverAgent, DiagnosticsAgent — deterministic `_run_async_impl` with internal LiteLLM calls). `type` field is always `llm` or `custom`; hybrid is a behavioral subcategory, not a third type. Four hybrid patterns defined. | 2026-03-10 |
| 57 | MemoryLoaderAgent formalized (extends #19) | Formalizes memory context loading as a deterministic CustomAgent in the pipeline (not an LLM-discretionary tool). Parallel to SkillLoaderAgent pattern — deterministic agents for pipeline-start context assembly, tools for on-demand retrieval. | 2026-03-10 |
| 58 | Agent security model formalized (extends #6, #33) | Formalizes the always-intended security boundaries: (1) Project-scope definition files restricted to `type: llm` only, `tool_role` ceiling validated against workflow manifest. (2) State key authorization via tier prefixes (`director:`, `pm:`, `worker:`) — EventPublisher ACL validates author tier matches prefix on `state_delta` keys. | 2026-03-10 |
| 59 | Deliverable artifact format — filesystem + pluggable ArtifactStore | Artifacts are files on disk (git-tracked default) with metadata in PostgreSQL. Pluggable `ArtifactStore` interface so workflows can declare storage (git, cloud drive, external API). Rejected: JSON files (unnecessary serialization), SQLite (second DB tech), PostgreSQL BLOBs (bloat + no git ops), ADK save_artifact (session-scoped, couples to ADK). | 2026-03-11 |
| 60 | Spec decomposition — hybrid LLM + deterministic validation | PM/opus generates structured deliverable list; deterministic CustomAgent validates (no cycles, references resolve, required fields, unique names). Retry on failure (2-3 attempts), escalate if still broken. Rejected: pure LLM (deterministic problems need deterministic validation), multi-pass LLM refinement (premature complexity), template-driven (constrains LLM judgment). | 2026-03-11 |
| 61 | Regression strategy — tiered (smoke/targeted/full) | Smoke tests every batch, targeted tests for cross-module batches (dependency fan-out > threshold), full regression at TaskGroup boundaries. PM writes policy once at project start; RegressionTestAgent classifies tier deterministically. Rejected: full every batch (O(batches*tests)), random sampling (no guarantees), PM-decided per-batch (violates deterministic principle). | 2026-03-11 |
| 62 | Rewrite Automaker TS libs in Python | All 6 candidate libs operate in Python engine layer. 3 already covered (models, LiteLLM routing, InstructionAssembler). Remaining 3 (git-utils, dependency-resolver, path security) are standard Python. Port logic, not code. Rejected: TS reuse via Node subprocess (cross-language boundary for no benefit), transpilation (unmaintainable output). | 2026-03-11 |
| 63 | Agent role system — keep current 7 named roles | `ROLE_PERMISSIONS` dict with 7 entries (planner, coder, reviewer, fixer, pm, director, default). `tool_role` frontmatter is the declaration. `tool_role` ceiling (Decision #58) via set intersection. Rejected: tier-only (loses planner vs coder distinction), capability-based (indirection over direct lookup), hybrid tier+capability (doubles authorization surface). | 2026-03-11 |
| 64 | Durable execution — native ADK resume, no Temporal | ADK ResumabilityConfig + checkpoint hardening: startup recovery scan for crash orphans, ARQ heartbeat/timeout, idempotency fence. Zero new infrastructure. Rejected: Temporal (adds infrastructure + fights ADK execution model + solves distributed problems we don't have), custom saga engine (over-engineering). Revisit if multi-machine deployment emerges. | 2026-03-11 |
| 65 | Memory ingestion — per-batch + pre-recreation | Ingest at batch completion (after regression tests pass) and before context recreation (persist step). Batch N+1's MemoryLoaderAgent picks up batch N learnings. Amends state.md §9.2 triggers. Rejected: per-deliverable (parallel execution = no within-batch benefit), per-TaskGroup (batch N+1 blind to batch N), PM-configurable (premature knob). | 2026-03-11 |
| 66 | Auth strategy — tiered (localhost now, API key later) | Bind to 127.0.0.1 explicitly (security boundary). `AUTOBUILDER_API_KEY` middleware (~30 LOC) added in Phase 11 only for non-localhost. Startup warning if network-accessible without key. Rejected: API key now (friction for nonexistent threat), JWT (solves deferred multi-user), OAuth2/OIDC (wildly disproportionate). | 2026-03-11 |
| 67 | Director queue trigger — hybrid inline + cron fallback | Director checks queue inline during work sessions (after PM turns via supervision callback — zero latency, zero token cost for empty queue). ARQ cron fallback for idle periods (configurable interval, default 60s, only invokes Director if pending items exist). Rejected: cron only (60s latency during active sessions), inline only (blocks when no work session), event-driven pub/sub (new infrastructure pattern for one use case). | 2026-03-11 |
| 68 | Director formation via Settings conversation | Director personality and CEO relationship formed through a dedicated Settings conversation (`ChatType.SETTINGS`). Three structured artifacts in `user:` scope: `user:director_identity`, `user:ceo_profile`, `user:operating_contract`. Auto-created on first access; formation is ~5-10 professional questions. Artifacts map to InstructionAssembler fragment types (IDENTITY, PROJECT, GOVERNANCE). Director can propose artifact updates from any session via CEO queue. Database-only storage (user: scope state via DatabaseSessionService). Rejected: static config file (mechanical, no relationship), single personality string (doesn't separate concerns), file-based artifacts (diverges from gateway-API-only persistence). | 2026-03-11 |
| 69 | Unify memory injection via InstructionAssembler (Phase 9) | Memory currently injected via ADK `{memory_context}` template placeholder (Layer 4 substitution), while skills use InstructionAssembler SKILL fragments. Asymmetric: skills get per-agent `applies_to` filtering, priority ordering, escape handling, and source auditing; memory gets none. When memory becomes real (Phase 9), migrate to assembler as a MEMORY fragment type — same pattern as skills. Enables per-agent memory filtering (reviewer doesn't need full project history), structured composition, and consistent auditability. ADK involvement in both loaders is minimal (BaseAgent inheritance, Event/state_delta persistence); the asymmetry is purely in our instruction composition layer. Acceptable tech debt until Phase 9. | 2026-03-11 |
| 70 | Manifest — progressive disclosure, stages, validators, completion reports | WORKFLOW.yaml manifest with progressive disclosure (2 fields minimum → full operating manual). Sections: stages, validators, resources, deliverable types, outputs, completion reports, brief templates, conventions, director guidance, config. Three pipeline types: single_pass, sequential, batch_parallel. `version` field for schema evolution. | PR-4 requires stage schemas, PR-5 requires per-stage agent config, PR-8 requires resource pre-validation, PR-11 requires declarative validator scheduling, PR-22 requires completion report structure. Progressive disclosure follows Prefect's principle: simple workflows have simple manifests. Research: Airflow TaskGroups, Prefect monoflows, K8s reconciliation pattern. | 2026-03-12 |
| 71 | Stage schema — organizational groupings, PM-driven transitions | Stages are organizational groupings within a single PM session (NOT separate execution contexts). PM drives transitions via reconciliation pattern (observe state delta, act). Per-stage scoping for agents, tools, skills, validators. Completion criteria compose as AND (deliverable status + validator results + approval gates). Stages and TaskGroups are distinct: stages are manifest-declared (CEO approves), TaskGroups are PM-created runtime planning artifacts (~1h work, Director approves). `stages` field is optional — omit for single-stage workflows. | Airflow deprecated SubDAGs (separate execution contexts for organizational grouping) in favor of TaskGroups. K8s operator pattern for state-driven transitions. Stages must not penalize simple workflows (Prefect progressive disclosure). Research: Airflow TaskGroup deprecation, K8s controllers, Temporal workflow-as-code. | 2026-03-12 |
| 72 | Workflow directory model — user-level overrides built-in by name | Two-tier: built-in (`app/workflows/`) + user (`~/.autobuilder/workflows/`, configurable via `AUTOBUILDER_WORKFLOWS_DIR`). No project-scoped workflows — workflows are selected for a project, not customized within one. User-level consistent with Director authoring being CEO-level activity. Cloud storage sync potential. Staging at `~/.autobuilder/workflows/.staging/`. | Workflows are fundamentally different from agents and skills: agents and skills are customized within a project; workflows are selected for a project at creation time. User-level (not project-level) matches the scope of workflow authoring as a CEO/Director activity. | 2026-03-12 |
| 73 | Pipeline.py generated from scratch by Director | Director writes pipeline.py from scratch guided by authoring skills (not from parameterized templates). Pipeline.py patterns provided as reference files within the `workflow-authoring` skill. CEO reviews pipeline code at activation gate. Import restrictions enforced at L4 validation (allowed modules only). | Templates constrain expressiveness to 3-4 patterns. Director (opus-class LLM) can compose ADK agent trees from first principles given reference examples. CEO review at activation gate mitigates the security risk of generated Python code. | 2026-03-12 |
| 74 | Validator framework — standard validators as evaluation functions | 10 standard validators ship with platform as evaluation functions dispatched by ValidatorRunner (refined by spec DD-2 — not CustomAgent classes; validators wrap existing agent output and record evidence). Validators are code (not skills). Skills teach the Director and PM how to design and compose validators. Director cannot create custom validators — it composes existing ones via manifest declarations. Validator types: deterministic, LLM, approval. Scheduling: per_deliverable, per_batch, per_taskgroup, per_stage. | Validators produce structured, machine-verifiable evidence for completion reports (PR-22). Deterministic validators must be deterministic code, not LLM-generated. Director composes from a palette of proven validators, not arbitrary code. | 2026-03-12 |
| 75 | Workflow quality framework — three-layer verification, deterministic close conditions | Three-layer completion reports (functional correctness, architectural conformance, contract completion) per PR-22. Deterministic TaskGroup/stage close conditions per PR-23 — hard gates that PM reasoning cannot override. Validator evidence feeds into completion report evidence_sources. Advisory vs blocking validators (required: true/false). | PR-22 mandates structured verification. PR-23 mandates deterministic close conditions. Separating machine evidence from LLM synthesis creates auditable completion reports. Hard gates prevent premature advancement. | 2026-03-12 |
| 76 | Director workflow authoring — 6-phase lifecycle with staging gate | Director creates workflows through 6-phase lifecycle: requirements gathering → resource discovery → draft composition → validation (5 layers) → CEO review → activation. CEO resource library via 5 thin query tools over existing registries (NOT a new Resource Library entity). Staging directory isolates drafts from active registry. Phase 7a = infrastructure, Phase 7b = Director authoring capability. Improvement loop via 4 feedback sources (execution logs, failure patterns, escalation history, workflow memory). | Director already has every primitive (file tools, conversation, opus reasoning). New design surface is conversation protocol, resource discovery, validation, and security boundaries. Thin query tools over existing registries avoid second source of truth. | 2026-03-12 |
| 77 | Workflow authoring skills — 9 skills across 4 categories | 9 workflow authoring skills: 1 Director-tier master skill (director-workflow-composition), 4 enhanced technical reference skills (workflow-authoring, agent-definition, skill-authoring, project-conventions), 2 quality framework skills (workflow-quality, workflow-testing), 2 domain pattern skills (software-development-patterns, research-patterns). Master skill cascades to 4 reference skills. Domain skills loaded on demand, not cascaded. Phase 7a ships 5 infrastructure skills, Phase 7b ships 4 Director authoring skills. | Master skill teaches the process; reference skills teach the format. Cascade chain keeps individual skills small and focused. Domain skills are large and domain-specific — always-loading them wastes context for non-matching workflow types. | 2026-03-12 |
