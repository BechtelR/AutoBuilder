# Memory
> Base: https://google.github.io/adk-docs

- **Memory service** `/sessions/memory/` — cross-session memory, MemoryService interface, InMemoryMemoryService
- **Artifacts** `/artifacts/` — binary/structured data storage, ArtifactService, session-scoped artifacts

## Key Classes
`MemoryService` `InMemoryMemoryService` `ArtifactService` `InMemoryArtifactService`

## See Also
→ state-sessions.md: session-scoped state (different from cross-session memory)
